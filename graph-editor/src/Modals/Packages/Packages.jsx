//import React from 'react';
//import UI from '../../UI';
//import { useDispatch, useSelector } from 'react-redux';
import './Packages.css';


function Packages() {
    /*let dispatch = useDispatch();
    let graphName = useSelector(state => state.graph.present.meta ? state.graph.present.meta.name : null);
    let open = useSelector(state => state.modals.packages);

    function handleClose() {
        dispatch({type: "SET_MODAL_OPEN", name: "packages", open: false});
    }

    return (
        <UI.Modal open={open} header={graphName + "'s Packages"}>
            <div className="Packages" style={{overflow: "auto"}}>
                <div className="packlist">

                </div>
                NOT IMPLEMENTED<br />Please use the terminal to install new packages
                <UI.Button onClick={handleClose}>Close</UI.Button>
            </div>
        </UI.Modal>
    );*/
    return null;
}

export default Packages;