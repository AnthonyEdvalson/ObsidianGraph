{
    "name": "new-project",
    "version": "0.0.0",
    "scripts": {
        "start": "nodemon",
        "wipe": "kill -9 $(lsof -t -i:5000)",
        "setup": "npm install && cd back && npm install && cd ../front && npm install"
    },
    "dependencies": {
        "concurrently": "^5.3.0",
        "express": "^4.17.1",
        "socket.io": "^3.0.3"
    }
}
